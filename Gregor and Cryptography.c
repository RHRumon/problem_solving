#include<stdio.h>
#define ll long long
int main()
{
    ll t;
    scanf("%lld",&t);
    while(t--)
    {
        ll p,i;
        scanf("%lld",&p);
        printf("2 %lld\n",p-1);

    }
    return 0;
}
